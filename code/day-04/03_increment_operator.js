console.log("Program starts");

let number = 0;
while (number < 10) {
  // console.log("Hello");
  console.log(number);
  // take the right side of "=" and put as new value to variable on the left
  // number = number + 1;

  // increment operator (shortcut) = syntactic sugar
  // number += 1;

  // ++ : increase number one time
  number++;
}
console.log("Program ends");
