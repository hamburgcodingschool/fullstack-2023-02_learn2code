function setup() {
    // changing the numbers will change width & height
    createCanvas(400, 400);
}

function draw() {
    // changing this number will change background grayscale
    // 0 -> black, 255 and bigger -> white
    background(220);
}