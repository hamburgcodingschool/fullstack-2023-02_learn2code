function setup() {
    // changing the numbers will change width & height
    createCanvas(400, 400);
}

function draw() {
    // when you use three numbers, the color will be mixed
    // according to the RGB color scheme (red, green, blue)
    background(100, 10, 200);
}